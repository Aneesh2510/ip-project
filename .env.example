# Firebase Configuration path
FIREBASE_CREDENTIALS_PATH=firebase-adminsdk.json

# Server Settings
HOST=0.0.0.0
PORT=8000
DEBUG=True

# Detection Thresholds
MAX_REQUESTS_PER_MINUTE=100
MAX_PORTS_SCANNED=10
ML_CONFIDENCE_THRESHOLD=0.75

# Network Interface for Packet Capture (e.g., eth0, wlan0, or leave empty for default)
CAPTURE_INTERFACE=
